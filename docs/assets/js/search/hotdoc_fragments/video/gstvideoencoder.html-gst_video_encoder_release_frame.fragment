fragment_downloaded_cb({"url":"video/gstvideoencoder.html#gst_video_encoder_release_frame","fragment":"\n\tgst_video_encoder_release_frame\n\n\n\tGstVideo.VideoEncoder.prototype.release_frame\n\n\n\tGstVideo.VideoEncoder.release_frame\n\nRemoves frame from list of pending frames and releases it, similar\nto calling gst_video_encoder_finish_frame without a buffer attached\nto the frame, but does not post a QoS message or do any additional\nprocessing. Events from frame are moved to the pending events list.\nParameters:\na GstVideoEncoder\na GstVideoCodecFrame\n\n\tSince\t\t\t: 1.26\n\t\t\nRemoves frame from list of pending frames and releases it, similar\nto calling GstVideo.VideoEncoder.prototype.finish_frame without a buffer attached\nto the frame, but does not post a QoS message or do any additional\nprocessing. Events from frame are moved to the pending events list.\nParameters:\na GstVideo.VideoEncoder\na GstVideo.VideoCodecFrame\n\n\tSince\t\t\t: 1.26\n\t\t\nRemoves frame from list of pending frames and releases it, similar\nto calling GstVideo.VideoEncoder.finish_frame without a buffer attached\nto the frame, but does not post a QoS message or do any additional\nprocessing. Events from frame are moved to the pending events list.\nParameters:\na GstVideo.VideoEncoder\na GstVideo.VideoCodecFrame\n\n\tSince\t\t\t: 1.26\n\t\t\n"});