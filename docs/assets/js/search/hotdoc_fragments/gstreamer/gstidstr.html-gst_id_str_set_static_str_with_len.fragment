fragment_downloaded_cb({"url":"gstreamer/gstidstr.html#gst_id_str_set_static_str_with_len","fragment":"\n\tgst_id_str_set_static_str_with_len\n\n\n\tGst.IdStr.prototype.set_static_str_with_len\n\n\n\tGst.IdStr.set_static_str_with_len\n\nSets s to the string value of length len. value needs to be valid for the\nremaining lifetime of the process, e.g. has to be a static string.\nvalue does not have to be NUL-terminated and len should not include the\nNUL-terminator.\nParameters:\nA GstIdStr\nA string\nLength of the string\n\n\tSince\t\t\t: 1.26\n\t\t\nSets s to the string value of length len. value needs to be valid for the\nremaining lifetime of the process, e.g. has to be a static string.\nvalue does not have to be NUL-terminated and len should not include the\nNUL-terminator.\nParameters:\nA Gst.IdStr\nA string\nLength of the string\n\n\tSince\t\t\t: 1.26\n\t\t\nSets s to the string value of length len. value needs to be valid for the\nremaining lifetime of the process, e.g. has to be a static string.\nvalue does not have to be NUL-terminated and len should not include the\nNUL-terminator.\nParameters:\nA Gst.IdStr\nA string\nLength of the string\n\n\tSince\t\t\t: 1.26\n\t\t\n"});