fragment_downloaded_cb({"url":"gstreamer/gststructure.html#gst_structure_map_in_place_id_str","fragment":"\n\tgst_structure_map_in_place_id_str\n\n\n\tGst.Structure.prototype.map_in_place_id_str\n\n\n\tGst.Structure.map_in_place_id_str\n\nCalls the provided function once for each field in the GstStructure. In\ncontrast to gst_structure_foreach_id_str, the function may modify but not delete the\nfields. The structure must be mutable.\nParameters:\na GstStructure\na function to call for each field\nprivate data\nTRUE if the supplied function returns TRUE For each of the fields,\nFALSE otherwise.\n\n\tSince\t\t\t: 1.26\n\t\t\nCalls the provided function once for each field in the Gst.Structure. In\ncontrast to Gst.Structure.prototype.foreach_id_str, the function may modify but not delete the\nfields. The structure must be mutable.\nParameters:\na Gst.Structure\na function to call for each field\nprivate data\ntrue if the supplied function returns true For each of the fields,\nfalse otherwise.\n\n\tSince\t\t\t: 1.26\n\t\t\nCalls the provided function once for each field in the Gst.Structure. In\ncontrast to Gst.Structure.foreach_id_str, the function may modify but not delete the\nfields. The structure must be mutable.\nParameters:\na Gst.Structure\na function to call for each field\nprivate data\nTrue if the supplied function returns True For each of the fields,\nFalse otherwise.\n\n\tSince\t\t\t: 1.26\n\t\t\n"});