fragment_downloaded_cb({"url":"gstreamer/gstinfo.html#gst_debug_print_object","fragment":"\n\tgst_debug_print_object\n\n\n\tGst.prototype.debug_print_object\n\n\n\tGst.debug_print_object\n\nReturns a string that represents ptr. This is safe to call with\nGstStructure, GstCapsFeatures, GstMiniObject s (e.g. GstCaps,\nGstBuffer or GstMessage), and GObjects (e.g. GstElement or GstPad).\nThe string representation is meant to be used for debugging purposes and\nmight change between GStreamer versions.\nPassing other kind of pointers might or might not work and is generally\nunsafe to do.\nParameters:\nthe object\na string containing a string\nrepresentation of the object\n\n\tSince\t\t\t: 1.26\n\t\t\nReturns a string that represents ptr. This is safe to call with\nGst.Structure, Gst.CapsFeatures, Gst.MiniObject s (e.g. Gst.Caps,\nGst.Buffer or Gst.Message), and GObjects (e.g. Gst.Element or Gst.Pad).\nThe string representation is meant to be used for debugging purposes and\nmight change between GStreamer versions.\nPassing other kind of pointers might or might not work and is generally\nunsafe to do.\nParameters:\nthe object\na string containing a string\nrepresentation of the object\n\n\tSince\t\t\t: 1.26\n\t\t\nReturns a string that represents ptr. This is safe to call with\nGst.Structure, Gst.CapsFeatures, Gst.MiniObject s (e.g. Gst.Caps,\nGst.Buffer or Gst.Message), and GObjects (e.g. Gst.Element or Gst.Pad).\nThe string representation is meant to be used for debugging purposes and\nmight change between GStreamer versions.\nPassing other kind of pointers might or might not work and is generally\nunsafe to do.\nParameters:\nthe object\na string containing a string\nrepresentation of the object\n\n\tSince\t\t\t: 1.26\n\t\t\n"});