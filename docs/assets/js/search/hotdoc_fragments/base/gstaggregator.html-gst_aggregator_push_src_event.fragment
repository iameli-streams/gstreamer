fragment_downloaded_cb({"url":"base/gstaggregator.html#gst_aggregator_push_src_event","fragment":"\n\tgst_aggregator_push_src_event\n\n\n\tGstBase.Aggregator.prototype.push_src_event\n\n\n\tGstBase.Aggregator.push_src_event\n\nThis method will push the provided event downstream. If needed, mandatory\nevents such as stream-start, caps, and segment events will be sent before\npushing the event.\nThis API does not allow pushing stream-start, caps, segment and EOS events.\nSpecific API like gst_aggregator_set_src_caps should be used for these.\nParameters:\nThe GstAggregator\nthe GstEvent to push.\n\n\tSince\t\t\t: 1.26\n\t\t\nThis method will push the provided event downstream. If needed, mandatory\nevents such as stream-start, caps, and segment events will be sent before\npushing the event.\nThis API does not allow pushing stream-start, caps, segment and EOS events.\nSpecific API like GstBase.Aggregator.prototype.set_src_caps should be used for these.\nParameters:\nThe GstBase.Aggregator\nthe Gst.Event to push.\n\n\tSince\t\t\t: 1.26\n\t\t\nThis method will push the provided event downstream. If needed, mandatory\nevents such as stream-start, caps, and segment events will be sent before\npushing the event.\nThis API does not allow pushing stream-start, caps, segment and EOS events.\nSpecific API like GstBase.Aggregator.set_src_caps should be used for these.\nParameters:\nThe GstBase.Aggregator\nthe Gst.Event to push.\n\n\tSince\t\t\t: 1.26\n\t\t\n"});