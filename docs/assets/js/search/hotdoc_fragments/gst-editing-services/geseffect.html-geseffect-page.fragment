fragment_downloaded_cb({"url":"gst-editing-services/geseffect.html#geseffect-page","fragment":"GESEffect\nAny GStreamer filter can be used as effects in GES. The only restriction we\nhave is that effects element should have a single sinkpad\n(which will be requested if necessary) and a single srcpad.\nNote that gesaudiomixer and gescompositor can be used as effects even\nthough they can have several sinkpads.\n"});